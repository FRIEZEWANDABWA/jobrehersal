# Copy to ".env.local" for local development (already ignored by git).
# On Vercel: Project → Settings → Environment Variables (same names).

# Required: 32+ random characters. Example: openssl rand -base64 32
AUTH_SECRET=

# Required: username and password you will type on /login
AUTH_USERNAME=
AUTH_PASSWORD=
